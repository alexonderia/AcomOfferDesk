services:
  max_bot:
    build: .
    container_name: max_bot_standalone
    env_file:
      - .env
    environment:
      BACKEND_BASE_URL: ${BACKEND_BASE_URL:-http://host.docker.internal:8000}
    restart: unless-stopped
