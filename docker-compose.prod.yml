services:
  # Production/test perimeter:
  # keep gateway internal by default, terminate TLS on external reverse proxy.
  gateway:
    ports: []

networks:
  project_net:
    name: project_net
    external: true
